-- This SQL script lists all records of the 'second_table' from the database 'hbtn_0c_0',
-- displaying both the score and name, ordered by score (top first)

SELECT score, name
FROM second_table
ORDER BY score DESC;
